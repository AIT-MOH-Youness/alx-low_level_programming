/**
 * Main - computes and prints the sum of all the multiples of 3 or 5 below 1024
 *
 * Return: value of the sum
 */
int main(void)
{
        int i,s=0,a=0;

	if (a < 1024)
	{	
        if ((a % 3)==0 || (a % 5)=0)
        s += a;

	}
	printf("%d", s);

        return (s);
}
